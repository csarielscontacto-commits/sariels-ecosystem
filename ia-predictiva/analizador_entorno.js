// Analizador de Entorno - Sariel's Bakery
function obtenerAjustePorClima() {
    const clima = "soleado"; // En el futuro, esto se conectará a internet
    return (clima === "lluvioso") ? 0.8 : 1.2;
}

